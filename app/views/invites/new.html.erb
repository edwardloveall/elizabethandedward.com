<%= simple_form_for @invite do |form| %>
  <%= form.input :email %>
  <%= form.association :people, as: :check_boxes %>
  <%= form.input :allow_plus_one %>
  <%= form.submit 'Send invite' %>
<% end %>
