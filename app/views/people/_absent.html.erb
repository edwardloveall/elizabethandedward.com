<div class="person absent">
  <%= link_to absent.name, attending_person_choice_path(absent.id, absent.choice.id, 'choice[attending]' => true), method: :put, remote: true, data: { type: 'html' }, class: 'attendance' %>
</div>
